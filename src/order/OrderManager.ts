import { Order } from "./order";

export class OrderManager {
    private orders : Order[]=[];

    getPriceFromOrder(){
        let price = 0;
        for(let order of this.orders){
            
        }
    }
}